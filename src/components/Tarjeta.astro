---
/** Tarjeta hairline. Base compartida por Servicios y Testimonios. */
---

<article class="tarjeta">
  <slot />
</article>

<style>
  .tarjeta {
    border: var(--b-hairline);
    padding: var(--e-16);
    border-radius: 0;
  }
</style>
